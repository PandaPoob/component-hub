# Component Hub

A modern React component library built with TypeScript, Vitest, shadcn/ui, and Storybook.

## 🚀 Tech Stack

- **React** - UI library
- **TypeScript** - Type safety
- **Vite** - Build tool and dev server
- **shadcn/ui** - Beautiful, accessible components
- **Tailwind CSS** - Utility-first CSS framework
- **Vitest** - Fast unit testing
- **Storybook** - Component documentation and development

## 📦 Installation

```bash
npm install
```

## 🛠️ Available Scripts

- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm run test` - Run tests with Vitest
- `npm run test:ui` - Open Vitest UI
- `npm run storybook` - Start Storybook
- `npm run build-storybook` - Build Storybook for production

## 🎨 Theme

Custom purple theme:
- **Text**: `#0B001A` (very dark purple)
- **Primary**: `#201036` (dark purple)
- **Background**: `#F2F1FF` (pastel purple)

## 📚 Components

### Button

A versatile button component with multiple variants and sizes.

View in Storybook: `npm run storybook`

## 🧪 Testing

Tests are written with Vitest and React Testing Library:

```bash
npm run test
```

## 📖 Storybook

Documentation and interactive component playground:

```bash
npm run storybook
```

Visit `http://localhost:6006` to view your components.
